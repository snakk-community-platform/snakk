/**
 * Head bundle — always-loaded scripts that must execute early.
 * Bundled in current tag order to preserve execution sequence.
 * Built by esbuild.core.mjs as an IIFE.
 */

import '../services/broadcast';
import '../core/loading-bar';
import '../core/actions';
import '../core/utils';
import '../core/auth';
import '../core/theme';
import '../core/scroll-nav';
