page "/unread"
@using Snakk.Web.Helpers
@using Snakk.Web.Pages.ViewModels
@using static Snakk.Web.Helpers.DisplayHelper
@model Snakk.Web.Pages.UnreadModel
@{
    ViewData["Title"] = T.Pages.UnreadTitle;
}

@section LeftColumn {
<partial name="Shared/_LeftSidebar" />
}

@if (!Model.IsAuthenticated)
{
    ViewData["Nudge.Title"] = T.Pages.UnreadTitle;
    ViewData["Nudge.Body"] = T.Pages.UnreadDescription;
    <div class="sn-center"><div class="sn-center-inner"><partial name="Shared/_RegistrationNudge" /></div></div>
    return;
}

<!-- Center Column -->
<div class="sn-center">
    <div class="sn-center-inner">
        <h2 class="sn-section-title">
            <span class="icon icon-bell" aria-hidden="true"></span>
            @T.Pages.UnreadHeading
        </h2>
        <p class="sn-section-description">@T.Pages.UnreadDescription</p>

        @if (Model.Discussions?.Items == null || !Model.Discussions.Items.Any())
        {
            <partial name="Shared/_EmptyState" model="new EmptyStateVM(T.Pages.UnreadEmpty)" />
        }
        else
        {
            <div id="discussions-container" class="h-feed">
                @foreach (var discussion in Model.Discussions.Items)
                {
                    @await Html.PartialAsync("Shared/_DiscussionCard",
                        new DiscussionListItemVM(discussion, Model.ShowCommunityInDiscussionList, Model.Community, GotoUnread: true))
                }

                @if (Model.Discussions.HasMoreItems && Model.NextCursor != null)
                {
                    <div hx-get="/partials/discussions?sort=since&cursor=@Model.NextCursor&pageSize=20"
                         hx-trigger="revealed once"
                         hx-swap="outerHTML"
                         hx-target="this"
                         hx-push-url="false"
                         class="py-2">
                        @for (var i = 0; i < 3; i++)
                        {
                            <div class="sn-card" style="opacity: 0.5">
                                <div class="sn-card-header">
                                    <div class="skeleton h-3 w-16 rounded"></div>
                                    <div class="skeleton h-4 w-4 rounded-full"></div>
                                    <div class="skeleton h-3 w-20 rounded"></div>
                                </div>
                                <div class="skeleton h-4 w-3/4 rounded mt-2"></div>
                                <div class="sn-card-stats mt-2">
                                    <div class="skeleton h-3 w-24 rounded"></div>
                                </div>
                            </div>
                        }
                    </div>
                }
                else
                {
                    <partial name="Shared/_EndOfScroll" />
                }
            </div>
        }
    </div>
</div>

<!-- Right Column -->
@{
    ViewData["RightPane.NewDiscussionUrl"] = "/create";
    if (Model.IsAuthenticated)
        ViewData["RightPane.Actions"] = new[] { "mark-all-read" };
    ViewData["RightPane.AboutTitle"] = T.Pages.UnreadAboutTitle;
    ViewData["RightPane.AboutDescription"] = T.Pages.UnreadAboutDesc;
}
<vc:global-right-sidebar />

@section Scripts {
    @if (Model.Discussions?.Items != null && Model.Discussions.Items.Any())
    {
        <script src="@SnakkUrlHelper.Js("pages/frontpage-discussions")" asp-append-version="true" defer></script>
    }
}
