@page "/c/{slug}/rules"
@using Snakk.Web.Helpers
@model Snakk.Web.Pages.Communities.RulesModel
@{
    ViewData["Title"] = $"{Model.CommunityDetail?.Name} Rules";
    ViewData["Description"] = $"Rules and guidelines for {Model.CommunityDetail?.Name}.";
}

@if (Model.CommunityDetail == null)
{
    <div class="max-w-xl mx-auto">
        <div class="clean-card">
            <div class="empty-state-simple">
                <h3>Community not found</h3>
                <p>This community may have been removed or the link might be incorrect.</p>
                <a href="/communities" class="btn btn-primary btn-sm mt-4">Back to Communities</a>
            </div>
        </div>
    </div>
}
else
{
    <div class="breadcrumb-bar">
        <nav class="breadcrumb">
            <a href="/">Home</a>
            <span class="breadcrumb-separator">/</span>
            <a href="@SnakkUrlHelper.Community(Model.CommunityDetail.Slug, Model.Community)">@Model.CommunityDetail.Name</a>
            <span class="breadcrumb-separator">/</span>
            <span>Rules</span>
        </nav>
    </div>

    <div class="max-w-3xl mx-auto px-4 py-8">
        <div class="page-header">
            <h1>@Model.CommunityDetail.Name Rules</h1>
            @if (!string.IsNullOrEmpty(Model.CommunityDetail.Description))
            {
                <p>@Model.CommunityDetail.Description</p>
            }
        </div>

        <div class="clean-card">
            @if (Model.Rules?.Rules == null || Model.Rules.Rules.Count == 0)
            {
                <div class="empty-state-simple">
                    <p>No rules have been defined for this community yet.</p>
                </div>
            }
            else
            {
                <div class="flex flex-col divide-y divide-base-200">
                    @foreach (var (rule, index) in Model.Rules.Rules.Select((r, i) => (r, i)))
                    {
                        <details class="group py-3 first:pt-0 last:pb-0">
                            <summary class="flex items-center justify-between cursor-pointer text-sm font-medium">
                                <span>@(index + 1). @rule.Title</span>
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24"
                                     fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                                     stroke-linejoin="round"
                                     class="flex-shrink-0 transition-transform group-open:rotate-180">
                                    <polyline points="6 9 12 15 18 9"></polyline>
                                </svg>
                            </summary>
                            <p class="text-sm text-base-content/70 pl-4 pt-2">@rule.Description</p>
                        </details>
                    }
                </div>
            }
        </div>

        <div class="mt-4 flex flex-col gap-2">
            <a href="/rules" class="text-sm hover:text-primary transition-colors">
                View Site Rules &rarr;
            </a>
        </div>
    </div>
}
