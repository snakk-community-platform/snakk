@page "/rules"
@using Snakk.Web.Helpers
@using Snakk.Web.Services
@inject ICommunityContext CommunityCtx
@model Snakk.Web.Pages.RulesModel
@{
    Layout = "_Layout";
    ViewData["Title"] = "Site Rules";
    ViewData["Description"] = "Rules and guidelines for the community.";
}

@section LeftColumn {
<partial name="Shared/_LeftSidebar" />
}

<div class="fp-center">
    <div class="fp-center-inner">
        <h2 class="fp-section-title">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
            Site Rules
        </h2>
        <p class="text-sm mb-6" style="color: var(--text-secondary)">Please follow these guidelines when participating.</p>

        @if (Model.Rules?.Rules == null || Model.Rules.Rules.Count == 0)
        {
            <p class="text-sm" style="color: var(--text-tertiary)">No site rules have been defined yet.</p>
        }
        else
        {
            <div class="flex flex-col gap-6">
                @foreach (var (rule, index) in Model.Rules.Rules.Select((r, i) => (r, i)))
                {
                    <div class="flex gap-4 items-start">
                        <span class="text-5xl font-black leading-none select-none shrink-0 w-14 text-right" style="color: var(--text-primary); opacity: 0.08">@(index + 1)</span>
                        <div class="pt-1 min-w-0">
                            <p class="text-sm font-semibold mb-1" style="color: var(--text-primary)">@rule.Title</p>
                            <p class="text-sm" style="color: var(--text-secondary)">@rule.Description</p>
                        </div>
                    </div>
                }
            </div>
        }
    </div>
</div>

@{
    ViewData["RightPane.NewDiscussionUrl"] = "/new";
}
<partial name="Shared/_GlobalRightSidebar" />
