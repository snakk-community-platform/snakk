@page "/rules"
@using Snakk.Web.Helpers
@using Snakk.Web.Services
@inject ICommunityContext CommunityCtx
@model Snakk.Web.Pages.RulesModel
@{
    ViewData["Title"] = T.Pages.RulesTitle;
    ViewData["Description"] = "Rules and guidelines for the community.";
}

@section LeftColumn {
<partial name="Shared/_LeftSidebar" />
}

<div class="sn-center">
    <div class="sn-center-inner">
        <h2 class="sn-section-title">
            <span class="icon icon-shield-check" aria-hidden="true"></span>
            @T.Pages.RulesHeading
        </h2>
        <p class="sn-text-sm sn-mb-6" style="color: var(--text-secondary)">@T.Pages.RulesLead</p>

        @if (Model.Rules?.Rules == null || Model.Rules.Rules.Count == 0)
        {
            <p class="sn-text-sm" style="color: var(--text-tertiary)">@T.Pages.RulesEmpty</p>
        }
        else
        {
            <div class="sn-rules-list">
                @foreach (var (rule, index) in Model.Rules.Rules.Select((r, i) => (r, i)))
                {
                    <div class="sn-rule-item">
                        <span class="sn-text-5xl sn-font-black leading-none sn-select-none sn-shrink-0 w-14 sn-text-right" style="color: var(--text-primary); opacity: 0.08">@(index + 1)</span>
                        <div class="sn-pt-1 sn-min-w-0">
                            <p class="sn-rule-title--rules" style="color: var(--text-primary)">@rule.Title</p>
                            <p class="sn-text-sm" style="color: var(--text-secondary)">@rule.Description</p>
                        </div>
                    </div>
                }
            </div>
        }
    </div>
</div>

@{
    ViewData["RightPane.NewDiscussionUrl"] = "/create";
}
<vc:global-right-sidebar />
