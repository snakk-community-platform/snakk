@page "/partials/following-discussions"
@using Snakk.Web.Pages.ViewModels
@model Snakk.Web.Pages.Partials.FollowingDiscussionsModel
@{
    Layout = null;
}

@foreach (var discussion in Model.Items)
{
    @await Html.PartialAsync("Shared/_DiscussionCard",
        new DiscussionListItemVM(discussion, Model.ShowCommunity, Model.Community, UnfollowId: discussion.PublicId))
}

@if (Model.HasMoreItems)
{
    <div hx-get="/partials/following-discussions?offset=@Model.NextOffset&pageSize=10"
         hx-trigger="revealed once"
         hx-swap="outerHTML"
         hx-target="this"
         hx-push-url="false"
         class="py-2">
        @for (var i = 0; i < 3; i++)
        {
            <div class="sn-card" style="opacity: 0.5">
                <div class="sn-card-header">
                    <div class="skeleton h-3 w-16 rounded"></div>
                    <div class="skeleton h-4 w-4 rounded-full"></div>
                    <div class="skeleton h-3 w-20 rounded"></div>
                </div>
                <div class="skeleton h-4 w-3/4 rounded mt-2"></div>
                <div class="sn-card-stats mt-2">
                    <div class="skeleton h-3 w-24 rounded"></div>
                </div>
            </div>
        }
    </div>
}
else if (Model.Items.Any() && Model.Offset > 0)
{
    <partial name="Shared/_EndOfScroll" />
}
