@page "/partials/my-discussions"
@using Snakk.Web.Pages.ViewModels
@model Snakk.Web.Pages.Partials.MyDiscussionsModel
@{
    Layout = null;
}

@foreach (var discussion in Model.Items)
{
    @await Html.PartialAsync("Shared/_DiscussionCard",
        new DiscussionListItemVM(discussion, Model.ShowCommunity, Model.Community))
}

@if (Model.HasMoreItems)
{
    <div hx-get="/partials/my-discussions?offset=@Model.NextOffset&pageSize=20"
         hx-trigger="revealed once"
         hx-swap="outerHTML"
         hx-target="this"
         hx-push-url="false"
         class="py-2">
        <partial name="Shared/_DiscussionSkeleton" />
    </div>
}
else if (Model.Offset > 0)
{
    <partial name="Shared/_EndOfScroll" />
}
