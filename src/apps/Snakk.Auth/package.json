{
  "name": "snakk.auth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:css",
    "build:css": "npx @tailwindcss/cli -i Styles/input.css -o wwwroot/css/auth.css --minify",
    "watch:css": "npx @tailwindcss/cli -i Styles/input.css -o wwwroot/css/auth.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "daisyui": "^5.0.0"
  }
}
